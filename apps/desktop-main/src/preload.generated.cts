// @generated by scripts/gen-preload.ts. Do not edit by hand.
import { contextBridge, ipcRenderer } from 'electron';
import { allChannels } from '@lucid-fin/contracts-parse';

interface GeneratedRequestSchema {
  parse(value: unknown): unknown;
}

const requestSchemas = new Map<string, GeneratedRequestSchema>();
for (const definition of allChannels) {
  if ('request' in definition.schemas) {
    requestSchemas.set(definition.channel, definition.schemas.request);
  }
}

function parseRequest(channel: string, request: unknown): unknown {
  const schema = requestSchemas.get(channel);
  if (!schema) {
    throw new Error(`Missing request schema for generated IPC channel: ${channel}`);
  }
  return schema.parse(request);
}

contextBridge.exposeInMainWorld('lucidAPI', {
  ipc: {
    cancel: (invocationId: string) => ipcRenderer.invoke('ipc:cancel', invocationId),
    onInvocation: (channel: string, cb: (invocationId: string) => void) => {
      const listener = (_e: unknown, payload: { invocationId: string }) => cb(payload.invocationId);
      ipcRenderer.on(`${channel}:invocation`, listener);
      return () => ipcRenderer.removeListener(`${channel}:invocation`, listener);
    },
    onEvent: (channel: string, cb: (payload: { invocationId: string; event: unknown }) => void) => {
      const listener = (_e: unknown, payload: { invocationId: string; event: unknown }) =>
        cb(payload);
      ipcRenderer.on(`${channel}:event`, listener);
      return () => ipcRenderer.removeListener(`${channel}:event`, listener);
    },
  },
  app: {
    onInitError: (cb: (payload: unknown) => void) => {
      const listener = (_e: unknown, payload: unknown) => cb(payload);
      ipcRenderer.on('app:init-error', listener);
      return () => ipcRenderer.removeListener('app:init-error', listener);
    },
    onReady: (cb: (payload: unknown) => void) => {
      const listener = (_e: unknown, payload: unknown) => cb(payload);
      ipcRenderer.on('app:ready', listener);
      return () => ipcRenderer.removeListener('app:ready', listener);
    },
    restart: async (req: unknown) => {
      const parsed = parseRequest('app:restart', req);
      return ipcRenderer.invoke('app:restart', parsed);
    },
    version: async (req: unknown) => {
      const parsed = parseRequest('app:version', req);
      return ipcRenderer.invoke('app:version', parsed);
    },
  },
  assetContent: {
    export: async (req: unknown) => {
      const parsed = parseRequest('assetContent:export', req);
      return ipcRenderer.invoke('assetContent:export', parsed);
    },
    getPath: async (req: unknown) => {
      const parsed = parseRequest('assetContent:getPath', req);
      return ipcRenderer.invoke('assetContent:getPath', parsed);
    },
    inspect: async (req: unknown) => {
      const parsed = parseRequest('assetContent:inspect', req);
      return ipcRenderer.invoke('assetContent:inspect', parsed);
    },
  },
  assetEntry: {
    copy: async (req: unknown) => {
      const parsed = parseRequest('assetEntry:copy', req);
      return ipcRenderer.invoke('assetEntry:copy', parsed);
    },
    delete: async (req: unknown) => {
      const parsed = parseRequest('assetEntry:delete', req);
      return ipcRenderer.invoke('assetEntry:delete', parsed);
    },
    import: async (req: unknown) => {
      const parsed = parseRequest('assetEntry:import', req);
      return ipcRenderer.invoke('assetEntry:import', parsed);
    },
    importBuffer: async (req: unknown) => {
      const parsed = parseRequest('assetEntry:importBuffer', req);
      return ipcRenderer.invoke('assetEntry:importBuffer', parsed);
    },
    move: async (req: unknown) => {
      const parsed = parseRequest('assetEntry:move', req);
      return ipcRenderer.invoke('assetEntry:move', parsed);
    },
    pickFile: async (req: unknown) => {
      const parsed = parseRequest('assetEntry:pickFile', req);
      return ipcRenderer.invoke('assetEntry:pickFile', parsed);
    },
    query: async (req: unknown) => {
      const parsed = parseRequest('assetEntry:query', req);
      return ipcRenderer.invoke('assetEntry:query', parsed);
    },
    rename: async (req: unknown) => {
      const parsed = parseRequest('assetEntry:rename', req);
      return ipcRenderer.invoke('assetEntry:rename', parsed);
    },
  },
  canvas: {
    create: async (req: unknown) => {
      const parsed = parseRequest('canvas:create', req);
      return ipcRenderer.invoke('canvas:create', parsed);
    },
    delete: async (req: unknown) => {
      const parsed = parseRequest('canvas:delete', req);
      return ipcRenderer.invoke('canvas:delete', parsed);
    },
    deletePermanent: async (req: unknown) => {
      const parsed = parseRequest('canvas:deletePermanent', req);
      return ipcRenderer.invoke('canvas:deletePermanent', parsed);
    },
    list: async (req: unknown) => {
      const parsed = parseRequest('canvas:list', req);
      return ipcRenderer.invoke('canvas:list', parsed);
    },
    load: async (req: unknown) => {
      const parsed = parseRequest('canvas:load', req);
      return ipcRenderer.invoke('canvas:load', parsed);
    },
    loadAll: async (req: unknown) => {
      const parsed = parseRequest('canvas:loadAll', req);
      return ipcRenderer.invoke('canvas:loadAll', parsed);
    },
    patch: async (req: unknown) => {
      const parsed = parseRequest('canvas:patch', req);
      return ipcRenderer.invoke('canvas:patch', parsed);
    },
    rename: async (req: unknown) => {
      const parsed = parseRequest('canvas:rename', req);
      return ipcRenderer.invoke('canvas:rename', parsed);
    },
    restore: async (req: unknown) => {
      const parsed = parseRequest('canvas:restore', req);
      return ipcRenderer.invoke('canvas:restore', parsed);
    },
    save: async (req: unknown) => {
      const parsed = parseRequest('canvas:save', req);
      return ipcRenderer.invoke('canvas:save', parsed);
    },
  },
  canvasDelivery: {
    update: async (req: unknown) => {
      const parsed = parseRequest('canvasDelivery:update', req);
      return ipcRenderer.invoke('canvasDelivery:update', parsed);
    },
  },
  character: {
    copy: async (req: unknown) => {
      const parsed = parseRequest('character:copy', req);
      return ipcRenderer.invoke('character:copy', parsed);
    },
    delete: async (req: unknown) => {
      const parsed = parseRequest('character:delete', req);
      return ipcRenderer.invoke('character:delete', parsed);
    },
    deleteLoadout: async (req: unknown) => {
      const parsed = parseRequest('character:deleteLoadout', req);
      return ipcRenderer.invoke('character:deleteLoadout', parsed);
    },
    get: async (req: unknown) => {
      const parsed = parseRequest('character:get', req);
      return ipcRenderer.invoke('character:get', parsed);
    },
    list: async (req: unknown) => {
      const parsed = parseRequest('character:list', req);
      return ipcRenderer.invoke('character:list', parsed);
    },
    removeRefImage: async (req: unknown) => {
      const parsed = parseRequest('character:removeRefImage', req);
      return ipcRenderer.invoke('character:removeRefImage', parsed);
    },
    save: async (req: unknown) => {
      const parsed = parseRequest('character:save', req);
      return ipcRenderer.invoke('character:save', parsed);
    },
    saveLoadout: async (req: unknown) => {
      const parsed = parseRequest('character:saveLoadout', req);
      return ipcRenderer.invoke('character:saveLoadout', parsed);
    },
    setFolder: async (req: unknown) => {
      const parsed = parseRequest('character:setFolder', req);
      return ipcRenderer.invoke('character:setFolder', parsed);
    },
    setRefImage: async (req: unknown) => {
      const parsed = parseRequest('character:setRefImage', req);
      return ipcRenderer.invoke('character:setRefImage', parsed);
    },
  },
  clipboard: {
    onAiDetected: (cb: (payload: unknown) => void) => {
      const listener = (_e: unknown, payload: unknown) => cb(payload);
      ipcRenderer.on('clipboard:ai-detected', listener);
      return () => ipcRenderer.removeListener('clipboard:ai-detected', listener);
    },
    setEnabled: async (req: unknown) => {
      const parsed = parseRequest('clipboard:setEnabled', req);
      return ipcRenderer.invoke('clipboard:setEnabled', parsed);
    },
  },
  colorStyle: {
    delete: async (req: unknown) => {
      const parsed = parseRequest('colorStyle:delete', req);
      return ipcRenderer.invoke('colorStyle:delete', parsed);
    },
    extract: async (req: unknown) => {
      const parsed = parseRequest('colorStyle:extract', req);
      return ipcRenderer.invoke('colorStyle:extract', parsed);
    },
    list: async (req: unknown) => {
      const parsed = parseRequest('colorStyle:list', req);
      return ipcRenderer.invoke('colorStyle:list', parsed);
    },
    save: async (req: unknown) => {
      const parsed = parseRequest('colorStyle:save', req);
      return ipcRenderer.invoke('colorStyle:save', parsed);
    },
  },
  commander: {
    cancel: async (req: unknown) => {
      const parsed = parseRequest('commander:cancel', req);
      return ipcRenderer.invoke('commander:cancel', parsed);
    },
    cancelStep: async (req: unknown) => {
      const parsed = parseRequest('commander:cancel-step', req);
      return ipcRenderer.invoke('commander:cancel-step', parsed);
    },
    onCanvasDispatch: (cb: (payload: unknown) => void) => {
      const listener = (_e: unknown, payload: unknown) => cb(payload);
      ipcRenderer.on('commander:canvas:dispatch', listener);
      return () => ipcRenderer.removeListener('commander:canvas:dispatch', listener);
    },
    compact: async (req: unknown) => {
      const parsed = parseRequest('commander:compact', req);
      return ipcRenderer.invoke('commander:compact', parsed);
    },
    onEntitiesUpdated: (cb: (payload: unknown) => void) => {
      const listener = (_e: unknown, payload: unknown) => cb(payload);
      ipcRenderer.on('commander:entities:updated', listener);
      return () => ipcRenderer.removeListener('commander:entities:updated', listener);
    },
    eventsHydrate: async (req: unknown) => {
      const parsed = parseRequest('commander:events:hydrate', req);
      return ipcRenderer.invoke('commander:events:hydrate', parsed);
    },
    injectMessage: async (req: unknown) => {
      const parsed = parseRequest('commander:inject-message', req);
      return ipcRenderer.invoke('commander:inject-message', parsed);
    },
    runControl: async (req: unknown) => {
      const parsed = parseRequest('commander:run:control', req);
      return ipcRenderer.invoke('commander:run:control', parsed);
    },
    runGet: async (req: unknown) => {
      const parsed = parseRequest('commander:run:get', req);
      return ipcRenderer.invoke('commander:run:get', parsed);
    },
    runTree: async (req: unknown) => {
      const parsed = parseRequest('commander:run:tree', req);
      return ipcRenderer.invoke('commander:run:tree', parsed);
    },
    onSettingsDispatch: (cb: (payload: unknown) => void) => {
      const listener = (_e: unknown, payload: unknown) => cb(payload);
      ipcRenderer.on('commander:settings:dispatch', listener);
      return () => ipcRenderer.removeListener('commander:settings:dispatch', listener);
    },
    start: async (req: unknown) => {
      const parsed = parseRequest('commander:start', req);
      return ipcRenderer.invoke('commander:start', parsed);
    },
    onStream: (cb: (payload: unknown) => void) => {
      const listener = (_e: unknown, payload: unknown) => cb(payload);
      ipcRenderer.on('commander:stream', listener);
      return () => ipcRenderer.removeListener('commander:stream', listener);
    },
    toolAnswer: async (req: unknown) => {
      const parsed = parseRequest('commander:tool:answer', req);
      return ipcRenderer.invoke('commander:tool:answer', parsed);
    },
    toolDecision: async (req: unknown) => {
      const parsed = parseRequest('commander:tool:decision', req);
      return ipcRenderer.invoke('commander:tool:decision', parsed);
    },
  },
  deliveryPackage: {
    cancel: async (req: unknown) => {
      const parsed = parseRequest('deliveryPackage:cancel', req);
      return ipcRenderer.invoke('deliveryPackage:cancel', parsed);
    },
    open: async (req: unknown) => {
      const parsed = parseRequest('deliveryPackage:open', req);
      return ipcRenderer.invoke('deliveryPackage:open', parsed);
    },
    retry: async (req: unknown) => {
      const parsed = parseRequest('deliveryPackage:retry', req);
      return ipcRenderer.invoke('deliveryPackage:retry', parsed);
    },
    start: async (req: unknown) => {
      const parsed = parseRequest('deliveryPackage:start', req);
      return ipcRenderer.invoke('deliveryPackage:start', parsed);
    },
    status: async (req: unknown) => {
      const parsed = parseRequest('deliveryPackage:status', req);
      return ipcRenderer.invoke('deliveryPackage:status', parsed);
    },
  },
  equipment: {
    copy: async (req: unknown) => {
      const parsed = parseRequest('equipment:copy', req);
      return ipcRenderer.invoke('equipment:copy', parsed);
    },
    delete: async (req: unknown) => {
      const parsed = parseRequest('equipment:delete', req);
      return ipcRenderer.invoke('equipment:delete', parsed);
    },
    get: async (req: unknown) => {
      const parsed = parseRequest('equipment:get', req);
      return ipcRenderer.invoke('equipment:get', parsed);
    },
    list: async (req: unknown) => {
      const parsed = parseRequest('equipment:list', req);
      return ipcRenderer.invoke('equipment:list', parsed);
    },
    removeRefImage: async (req: unknown) => {
      const parsed = parseRequest('equipment:removeRefImage', req);
      return ipcRenderer.invoke('equipment:removeRefImage', parsed);
    },
    save: async (req: unknown) => {
      const parsed = parseRequest('equipment:save', req);
      return ipcRenderer.invoke('equipment:save', parsed);
    },
    setFolder: async (req: unknown) => {
      const parsed = parseRequest('equipment:setFolder', req);
      return ipcRenderer.invoke('equipment:setFolder', parsed);
    },
    setRefImage: async (req: unknown) => {
      const parsed = parseRequest('equipment:setRefImage', req);
      return ipcRenderer.invoke('equipment:setRefImage', parsed);
    },
  },
  ffmpeg: {
    probe: async (req: unknown) => {
      const parsed = parseRequest('ffmpeg:probe', req);
      return ipcRenderer.invoke('ffmpeg:probe', parsed);
    },
    thumbnail: async (req: unknown) => {
      const parsed = parseRequest('ffmpeg:thumbnail', req);
      return ipcRenderer.invoke('ffmpeg:thumbnail', parsed);
    },
    transcode: async (req: unknown) => {
      const parsed = parseRequest('ffmpeg:transcode', req);
      return ipcRenderer.invoke('ffmpeg:transcode', parsed);
    },
  },
  folder: {
    assetCreate: async (req: unknown) => {
      const parsed = parseRequest('folder.asset:create', req);
      return ipcRenderer.invoke('folder.asset:create', parsed);
    },
    assetDelete: async (req: unknown) => {
      const parsed = parseRequest('folder.asset:delete', req);
      return ipcRenderer.invoke('folder.asset:delete', parsed);
    },
    assetList: async (req: unknown) => {
      const parsed = parseRequest('folder.asset:list', req);
      return ipcRenderer.invoke('folder.asset:list', parsed);
    },
    assetMove: async (req: unknown) => {
      const parsed = parseRequest('folder.asset:move', req);
      return ipcRenderer.invoke('folder.asset:move', parsed);
    },
    assetRename: async (req: unknown) => {
      const parsed = parseRequest('folder.asset:rename', req);
      return ipcRenderer.invoke('folder.asset:rename', parsed);
    },
    characterCreate: async (req: unknown) => {
      const parsed = parseRequest('folder.character:create', req);
      return ipcRenderer.invoke('folder.character:create', parsed);
    },
    characterDelete: async (req: unknown) => {
      const parsed = parseRequest('folder.character:delete', req);
      return ipcRenderer.invoke('folder.character:delete', parsed);
    },
    characterList: async (req: unknown) => {
      const parsed = parseRequest('folder.character:list', req);
      return ipcRenderer.invoke('folder.character:list', parsed);
    },
    characterMove: async (req: unknown) => {
      const parsed = parseRequest('folder.character:move', req);
      return ipcRenderer.invoke('folder.character:move', parsed);
    },
    characterRename: async (req: unknown) => {
      const parsed = parseRequest('folder.character:rename', req);
      return ipcRenderer.invoke('folder.character:rename', parsed);
    },
    equipmentCreate: async (req: unknown) => {
      const parsed = parseRequest('folder.equipment:create', req);
      return ipcRenderer.invoke('folder.equipment:create', parsed);
    },
    equipmentDelete: async (req: unknown) => {
      const parsed = parseRequest('folder.equipment:delete', req);
      return ipcRenderer.invoke('folder.equipment:delete', parsed);
    },
    equipmentList: async (req: unknown) => {
      const parsed = parseRequest('folder.equipment:list', req);
      return ipcRenderer.invoke('folder.equipment:list', parsed);
    },
    equipmentMove: async (req: unknown) => {
      const parsed = parseRequest('folder.equipment:move', req);
      return ipcRenderer.invoke('folder.equipment:move', parsed);
    },
    equipmentRename: async (req: unknown) => {
      const parsed = parseRequest('folder.equipment:rename', req);
      return ipcRenderer.invoke('folder.equipment:rename', parsed);
    },
    locationCreate: async (req: unknown) => {
      const parsed = parseRequest('folder.location:create', req);
      return ipcRenderer.invoke('folder.location:create', parsed);
    },
    locationDelete: async (req: unknown) => {
      const parsed = parseRequest('folder.location:delete', req);
      return ipcRenderer.invoke('folder.location:delete', parsed);
    },
    locationList: async (req: unknown) => {
      const parsed = parseRequest('folder.location:list', req);
      return ipcRenderer.invoke('folder.location:list', parsed);
    },
    locationMove: async (req: unknown) => {
      const parsed = parseRequest('folder.location:move', req);
      return ipcRenderer.invoke('folder.location:move', parsed);
    },
    locationRename: async (req: unknown) => {
      const parsed = parseRequest('folder.location:rename', req);
      return ipcRenderer.invoke('folder.location:rename', parsed);
    },
  },
  health: {
    ping: async (req: unknown) => {
      const parsed = parseRequest('health:ping', req);
      return ipcRenderer.invoke('health:ping', parsed);
    },
  },
  keychain: {
    delete: async (req: unknown) => {
      const parsed = parseRequest('keychain:delete', req);
      return ipcRenderer.invoke('keychain:delete', parsed);
    },
    getMasked: async (req: unknown) => {
      const parsed = parseRequest('keychain:getMasked', req);
      return ipcRenderer.invoke('keychain:getMasked', parsed);
    },
    isConfigured: async (req: unknown) => {
      const parsed = parseRequest('keychain:isConfigured', req);
      return ipcRenderer.invoke('keychain:isConfigured', parsed);
    },
    set: async (req: unknown) => {
      const parsed = parseRequest('keychain:set', req);
      return ipcRenderer.invoke('keychain:set', parsed);
    },
    test: async (req: unknown) => {
      const parsed = parseRequest('keychain:test', req);
      return ipcRenderer.invoke('keychain:test', parsed);
    },
  },
  location: {
    copy: async (req: unknown) => {
      const parsed = parseRequest('location:copy', req);
      return ipcRenderer.invoke('location:copy', parsed);
    },
    delete: async (req: unknown) => {
      const parsed = parseRequest('location:delete', req);
      return ipcRenderer.invoke('location:delete', parsed);
    },
    get: async (req: unknown) => {
      const parsed = parseRequest('location:get', req);
      return ipcRenderer.invoke('location:get', parsed);
    },
    list: async (req: unknown) => {
      const parsed = parseRequest('location:list', req);
      return ipcRenderer.invoke('location:list', parsed);
    },
    removeRefImage: async (req: unknown) => {
      const parsed = parseRequest('location:removeRefImage', req);
      return ipcRenderer.invoke('location:removeRefImage', parsed);
    },
    save: async (req: unknown) => {
      const parsed = parseRequest('location:save', req);
      return ipcRenderer.invoke('location:save', parsed);
    },
    setFolder: async (req: unknown) => {
      const parsed = parseRequest('location:setFolder', req);
      return ipcRenderer.invoke('location:setFolder', parsed);
    },
    setRefImage: async (req: unknown) => {
      const parsed = parseRequest('location:setRefImage', req);
      return ipcRenderer.invoke('location:setRefImage', parsed);
    },
  },
  logger: {
    onEntry: (cb: (payload: unknown) => void) => {
      const listener = (_e: unknown, payload: unknown) => cb(payload);
      ipcRenderer.on('logger:entry', listener);
      return () => ipcRenderer.removeListener('logger:entry', listener);
    },
    getRecent: async (req: unknown) => {
      const parsed = parseRequest('logger:getRecent', req);
      return ipcRenderer.invoke('logger:getRecent', parsed);
    },
  },
  preset: {
    delete: async (req: unknown) => {
      const parsed = parseRequest('preset:delete', req);
      return ipcRenderer.invoke('preset:delete', parsed);
    },
    export: async (req: unknown) => {
      const parsed = parseRequest('preset:export', req);
      return ipcRenderer.invoke('preset:export', parsed);
    },
    import: async (req: unknown) => {
      const parsed = parseRequest('preset:import', req);
      return ipcRenderer.invoke('preset:import', parsed);
    },
    list: async (req: unknown) => {
      const parsed = parseRequest('preset:list', req);
      return ipcRenderer.invoke('preset:list', parsed);
    },
    reset: async (req: unknown) => {
      const parsed = parseRequest('preset:reset', req);
      return ipcRenderer.invoke('preset:reset', parsed);
    },
    save: async (req: unknown) => {
      const parsed = parseRequest('preset:save', req);
      return ipcRenderer.invoke('preset:save', parsed);
    },
  },
  processPrompt: {
    get: async (req: unknown) => {
      const parsed = parseRequest('processPrompt:get', req);
      return ipcRenderer.invoke('processPrompt:get', parsed);
    },
    list: async (req: unknown) => {
      const parsed = parseRequest('processPrompt:list', req);
      return ipcRenderer.invoke('processPrompt:list', parsed);
    },
    reset: async (req: unknown) => {
      const parsed = parseRequest('processPrompt:reset', req);
      return ipcRenderer.invoke('processPrompt:reset', parsed);
    },
    setCustom: async (req: unknown) => {
      const parsed = parseRequest('processPrompt:setCustom', req);
      return ipcRenderer.invoke('processPrompt:setCustom', parsed);
    },
  },
  promptAssembly: {
    get: async (req: unknown) => {
      const parsed = parseRequest('promptAssembly:get', req);
      return ipcRenderer.invoke('promptAssembly:get', parsed);
    },
  },
  provider: {
    health: async (req: unknown) => {
      const parsed = parseRequest('provider:health', req);
      return ipcRenderer.invoke('provider:health', parsed);
    },
    healthGet: async (req: unknown) => {
      const parsed = parseRequest('provider:health:get', req);
      return ipcRenderer.invoke('provider:health:get', parsed);
    },
  },
  providerOAuth: {
    cancelLogin: async (req: unknown) => {
      const parsed = parseRequest('providerOAuth:cancelLogin', req);
      return ipcRenderer.invoke('providerOAuth:cancelLogin', parsed);
    },
    onChanged: (cb: (payload: unknown) => void) => {
      const listener = (_e: unknown, payload: unknown) => cb(payload);
      ipcRenderer.on('providerOAuth:changed', listener);
      return () => ipcRenderer.removeListener('providerOAuth:changed', listener);
    },
    login: async (req: unknown) => {
      const parsed = parseRequest('providerOAuth:login', req);
      return ipcRenderer.invoke('providerOAuth:login', parsed);
    },
    logout: async (req: unknown) => {
      const parsed = parseRequest('providerOAuth:logout', req);
      return ipcRenderer.invoke('providerOAuth:logout', parsed);
    },
    status: async (req: unknown) => {
      const parsed = parseRequest('providerOAuth:status', req);
      return ipcRenderer.invoke('providerOAuth:status', parsed);
    },
  },
  reviewCut: {
    cancel: async (req: unknown) => {
      const parsed = parseRequest('reviewCut:cancel', req);
      return ipcRenderer.invoke('reviewCut:cancel', parsed);
    },
    open: async (req: unknown) => {
      const parsed = parseRequest('reviewCut:open', req);
      return ipcRenderer.invoke('reviewCut:open', parsed);
    },
    start: async (req: unknown) => {
      const parsed = parseRequest('reviewCut:start', req);
      return ipcRenderer.invoke('reviewCut:start', parsed);
    },
    status: async (req: unknown) => {
      const parsed = parseRequest('reviewCut:status', req);
      return ipcRenderer.invoke('reviewCut:status', parsed);
    },
  },
  script: {
    import: async (req: unknown) => {
      const parsed = parseRequest('script:import', req);
      return ipcRenderer.invoke('script:import', parsed);
    },
    load: async (req: unknown) => {
      const parsed = parseRequest('script:load', req);
      return ipcRenderer.invoke('script:load', parsed);
    },
    parse: async (req: unknown) => {
      const parsed = parseRequest('script:parse', req);
      return ipcRenderer.invoke('script:parse', parsed);
    },
    save: async (req: unknown) => {
      const parsed = parseRequest('script:save', req);
      return ipcRenderer.invoke('script:save', parsed);
    },
  },
  session: {
    delete: async (req: unknown) => {
      const parsed = parseRequest('session:delete', req);
      return ipcRenderer.invoke('session:delete', parsed);
    },
    get: async (req: unknown) => {
      const parsed = parseRequest('session:get', req);
      return ipcRenderer.invoke('session:get', parsed);
    },
    list: async (req: unknown) => {
      const parsed = parseRequest('session:list', req);
      return ipcRenderer.invoke('session:list', parsed);
    },
    move: async (req: unknown) => {
      const parsed = parseRequest('session:move', req);
      return ipcRenderer.invoke('session:move', parsed);
    },
    upsert: async (req: unknown) => {
      const parsed = parseRequest('session:upsert', req);
      return ipcRenderer.invoke('session:upsert', parsed);
    },
  },
  settings: {
    load: async (req: unknown) => {
      const parsed = parseRequest('settings:load', req);
      return ipcRenderer.invoke('settings:load', parsed);
    },
    onProviderKeyUpdated: (cb: (payload: unknown) => void) => {
      const listener = (_e: unknown, payload: unknown) => cb(payload);
      ipcRenderer.on('settings:providerKeyUpdated', listener);
      return () => ipcRenderer.removeListener('settings:providerKeyUpdated', listener);
    },
    save: async (req: unknown) => {
      const parsed = parseRequest('settings:save', req);
      return ipcRenderer.invoke('settings:save', parsed);
    },
  },
  shell: {
    openExternal: async (req: unknown) => {
      const parsed = parseRequest('shell:openExternal', req);
      return ipcRenderer.invoke('shell:openExternal', parsed);
    },
  },
  snapshot: {
    capture: async (req: unknown) => {
      const parsed = parseRequest('snapshot:capture', req);
      return ipcRenderer.invoke('snapshot:capture', parsed);
    },
    delete: async (req: unknown) => {
      const parsed = parseRequest('snapshot:delete', req);
      return ipcRenderer.invoke('snapshot:delete', parsed);
    },
    list: async (req: unknown) => {
      const parsed = parseRequest('snapshot:list', req);
      return ipcRenderer.invoke('snapshot:list', parsed);
    },
    restore: async (req: unknown) => {
      const parsed = parseRequest('snapshot:restore', req);
      return ipcRenderer.invoke('snapshot:restore', parsed);
    },
  },
  storage: {
    backupDatabase: async (req: unknown) => {
      const parsed = parseRequest('storage:backupDatabase', req);
      return ipcRenderer.invoke('storage:backupDatabase', parsed);
    },
    clearLogs: async (req: unknown) => {
      const parsed = parseRequest('storage:clearLogs', req);
      return ipcRenderer.invoke('storage:clearLogs', parsed);
    },
    getOverview: async (req: unknown) => {
      const parsed = parseRequest('storage:getOverview', req);
      return ipcRenderer.invoke('storage:getOverview', parsed);
    },
    openFolder: async (req: unknown) => {
      const parsed = parseRequest('storage:openFolder', req);
      return ipcRenderer.invoke('storage:openFolder', parsed);
    },
    openPath: async (req: unknown) => {
      const parsed = parseRequest('storage:openPath', req);
      return ipcRenderer.invoke('storage:openPath', parsed);
    },
    pickFolder: async (req: unknown) => {
      const parsed = parseRequest('storage:pickFolder', req);
      return ipcRenderer.invoke('storage:pickFolder', parsed);
    },
    pickOpenFile: async (req: unknown) => {
      const parsed = parseRequest('storage:pickOpenFile', req);
      return ipcRenderer.invoke('storage:pickOpenFile', parsed);
    },
    pickSaveFile: async (req: unknown) => {
      const parsed = parseRequest('storage:pickSaveFile', req);
      return ipcRenderer.invoke('storage:pickSaveFile', parsed);
    },
    restoreDatabase: async (req: unknown) => {
      const parsed = parseRequest('storage:restoreDatabase', req);
      return ipcRenderer.invoke('storage:restoreDatabase', parsed);
    },
    showInFolder: async (req: unknown) => {
      const parsed = parseRequest('storage:showInFolder', req);
      return ipcRenderer.invoke('storage:showInFolder', parsed);
    },
    vacuumDatabase: async (req: unknown) => {
      const parsed = parseRequest('storage:vacuumDatabase', req);
      return ipcRenderer.invoke('storage:vacuumDatabase', parsed);
    },
  },
  style: {
    load: async (req: unknown) => {
      const parsed = parseRequest('style:load', req);
      return ipcRenderer.invoke('style:load', parsed);
    },
    save: async (req: unknown) => {
      const parsed = parseRequest('style:save', req);
      return ipcRenderer.invoke('style:save', parsed);
    },
  },
  taskLists: {
    approveGate: async (req: unknown) => {
      const parsed = parseRequest('taskList:approveGate', req);
      return ipcRenderer.invoke('taskList:approveGate', parsed);
    },
    cancelMedia: async (req: unknown) => {
      const parsed = parseRequest('taskList:cancelMedia', req);
      return ipcRenderer.invoke('taskList:cancelMedia', parsed);
    },
    get: async (req: unknown) => {
      const parsed = parseRequest('taskList:get', req);
      return ipcRenderer.invoke('taskList:get', parsed);
    },
    getDelivery: async (req: unknown) => {
      const parsed = parseRequest('taskList:getDelivery', req);
      return ipcRenderer.invoke('taskList:getDelivery', parsed);
    },
    getPendingApproval: async (req: unknown) => {
      const parsed = parseRequest('taskList:getPendingApproval', req);
      return ipcRenderer.invoke('taskList:getPendingApproval', parsed);
    },
    getTasks: async (req: unknown) => {
      const parsed = parseRequest('taskList:getTasks', req);
      return ipcRenderer.invoke('taskList:getTasks', parsed);
    },
    getVisualAuditions: async (req: unknown) => {
      const parsed = parseRequest('taskList:getVisualAuditions', req);
      return ipcRenderer.invoke('taskList:getVisualAuditions', parsed);
    },
    list: async (req: unknown) => {
      const parsed = parseRequest('taskList:list', req);
      return ipcRenderer.invoke('taskList:list', parsed);
    },
    listPendingDecisions: async (req: unknown) => {
      const parsed = parseRequest('taskList:listPendingDecisions', req);
      return ipcRenderer.invoke('taskList:listPendingDecisions', parsed);
    },
    rejectGate: async (req: unknown) => {
      const parsed = parseRequest('taskList:rejectGate', req);
      return ipcRenderer.invoke('taskList:rejectGate', parsed);
    },
    requestChanges: async (req: unknown) => {
      const parsed = parseRequest('taskList:requestChanges', req);
      return ipcRenderer.invoke('taskList:requestChanges', parsed);
    },
    requestVisualAuditionChanges: async (req: unknown) => {
      const parsed = parseRequest('taskList:requestVisualAuditionChanges', req);
      return ipcRenderer.invoke('taskList:requestVisualAuditionChanges', parsed);
    },
    retryMedia: async (req: unknown) => {
      const parsed = parseRequest('taskList:retryMedia', req);
      return ipcRenderer.invoke('taskList:retryMedia', parsed);
    },
    retryMediaEvaluation: async (req: unknown) => {
      const parsed = parseRequest('taskList:retryMediaEvaluation', req);
      return ipcRenderer.invoke('taskList:retryMediaEvaluation', parsed);
    },
    selectVisualCandidate: async (req: unknown) => {
      const parsed = parseRequest('taskList:selectVisualCandidate', req);
      return ipcRenderer.invoke('taskList:selectVisualCandidate', parsed);
    },
    startMedia: async (req: unknown) => {
      const parsed = parseRequest('taskList:startMedia', req);
      return ipcRenderer.invoke('taskList:startMedia', parsed);
    },
  },
  updater: {
    check: async (req: unknown) => {
      const parsed = parseRequest('updater:check', req);
      return ipcRenderer.invoke('updater:check', parsed);
    },
    download: async (req: unknown) => {
      const parsed = parseRequest('updater:download', req);
      return ipcRenderer.invoke('updater:download', parsed);
    },
    install: async (req: unknown) => {
      const parsed = parseRequest('updater:install', req);
      return ipcRenderer.invoke('updater:install', parsed);
    },
    onProgress: (cb: (payload: unknown) => void) => {
      const listener = (_e: unknown, payload: unknown) => cb(payload);
      ipcRenderer.on('updater:progress', listener);
      return () => ipcRenderer.removeListener('updater:progress', listener);
    },
    status: async (req: unknown) => {
      const parsed = parseRequest('updater:status', req);
      return ipcRenderer.invoke('updater:status', parsed);
    },
    onToast: (cb: (payload: unknown) => void) => {
      const listener = (_e: unknown, payload: unknown) => cb(payload);
      ipcRenderer.on('updater:toast', listener);
      return () => ipcRenderer.removeListener('updater:toast', listener);
    },
  },
  vision: {
    describeImage: async (req: unknown) => {
      const parsed = parseRequest('vision:describeImage', req);
      return ipcRenderer.invoke('vision:describeImage', parsed);
    },
  },
});
