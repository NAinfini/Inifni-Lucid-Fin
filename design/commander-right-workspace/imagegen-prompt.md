# Reference mockup generation

- Mode: built-in `image_gen`
- Use case: `ui-mockup`
- Final asset: `commander-right-workspace-1280x720.png`

## Final prompt

Create one high-fidelity 16:9 implementation reference for an existing Electron dark node-canvas application. Show the full 1280×720 viewport as a flat screenshot, with the existing node graph and narrow toolbar on the left and one full-height Commander AI dock on the right. The dock is approximately 560 px wide and separated by a visible divider plus an 8 px resize hit-area.

Preserve the existing DaVinci Resolve / After Effects inspired visual language: cool-neutral background `#0d1017`, card `#151923`, elevated surface `#1b202b`, border `#282e3b`, primary blue `#3b82f6`, amber `#f59e0b`, danger red `#dc2626`; Inter/system typography, compact 12–14 px UI, 6 px radii, thin borders, minimal shadow, no glassmorphism, no neon gradients, and no brand redesign.

Right dock structure: 44 px header with amber lightning, “Commander AI”, “Canvas · Neon Harbor”, overflow and close; flat top tabs “聊天 Chat” and “工作流 Workflow” with badge 2; compact live row “正在生成制作计划 · Step 4/8 · 00:18” and “停止”; persistent section “需要处理 3”. Within that section show three distinct items: a blue question card asking “Which version should be used for the final cut? 最终剪辑应采用哪个版本？” with “Version B — 保留当前节奏” and “其他…”; an amber tool authorization card “工具授权 · 项目变更” for “Update 12 shot nodes / 写入 12 个镜头节点” with “跳过” and “允许一次”; a stronger persistent workflow approval card with 2 px amber accent, “制作计划审批 · 阻塞中”, “Persistent workflow gate · Rev 3”, “Neon Harbor”, “12 scenes · 16:9 · 00:42 · $28.40 max”, “查看详情”, “请求修改”, and a focused solid-blue “批准并继续”.

Below attention, show a scrollable mixed Chinese/English assistant response, a collapsed disclosure “过程 · 6 个工具 · 1m 42s”, visible “查看工具记录”, and a three-dot menu. At the bottom show a sticky composer with placeholder “发送消息… 输入 / 浏览全部命令”, visible “命令” with `/`, attachment, “GPT-5”, “严格”, and a blue send button. Keep all controls implementation-realistic, readable, high contrast, and encoded with icon + label + color. Do not show another drawer, modal, floating Commander window, device frame, perspective, mobile UI, stock art, or light theme.
