import { readFile, writeFile } from 'node:fs/promises';
import { registerHooks } from 'node:module';
import { dirname, resolve } from 'node:path';
import { fileURLToPath, pathToFileURL } from 'node:url';
import { format } from 'prettier';

export interface TargetWireSource {
  readonly PUBLIC_WIRE_METHODS_V1: Readonly<Record<string, unknown>>;
  readonly TARGET_DESKTOP_API_GLOBAL_V1: string;
  readonly TARGET_WIRE_INVOKE_CHANNEL_V1: string;
  readonly TARGET_WIRE_PUSH_CHANNEL_V1: string;
}

export interface TargetPreloadArtifacts {
  readonly preload: string;
  readonly rendererTypes: string;
}

type GenerationMode = 'check' | 'write';

const repoRoot = resolve(dirname(fileURLToPath(import.meta.url)), '..');
const targetContractsSourceRoot = resolve(repoRoot, 'packages/target-contracts/src');
const targetContractsSourceUrl = `${pathToFileURL(targetContractsSourceRoot).href}/`;
const wireSourcePath = resolve(targetContractsSourceRoot, 'wire.ts');
const preloadPath = resolve(repoRoot, 'apps/desktop-main/src/target/preload.generated.cts');
const rendererTypesPath = resolve(targetContractsSourceRoot, 'desktop-api.generated.ts');
const prettierOptions = Object.freeze({
  parser: 'typescript',
  printWidth: 100,
  singleQuote: true,
  semi: true,
  trailingComma: 'all',
  endOfLine: 'lf',
} as const);

let sourceHookRegistered = false;

function registerTargetContractsSourceHook(): void {
  if (sourceHookRegistered) return;
  registerHooks({
    resolve(specifier, context, nextResolve) {
      try {
        return nextResolve(specifier, context);
      } catch (cause) {
        if (specifier.endsWith('.js') && context.parentURL?.startsWith(targetContractsSourceUrl)) {
          return nextResolve(`${specifier.slice(0, -3)}.ts`, context);
        }
        throw cause;
      }
    },
  });
  sourceHookRegistered = true;
}

async function loadTargetWireSource(): Promise<TargetWireSource> {
  registerTargetContractsSourceHook();
  return (await import(pathToFileURL(wireSourcePath).href)) as TargetWireSource;
}

function upperFirst(value: string): string {
  return value.length === 0 ? value : `${value[0]!.toUpperCase()}${value.slice(1)}`;
}

function lowerFirst(value: string): string {
  return value.length === 0 ? value : `${value[0]!.toLowerCase()}${value.slice(1)}`;
}

interface PreparedMethod {
  readonly method: string;
  readonly namespace: string;
  readonly apiMethod: string;
}

function prepareMethods(methods: readonly string[]): readonly PreparedMethod[] {
  const seenMethods = new Set<string>();
  const prepared = methods.map((method) => {
    const [namespace, ...parts] = method.split('.');
    if (namespace === undefined || namespace.length === 0 || parts.length === 0) {
      throw new Error(`Target Wire method must contain a namespace: ${method}`);
    }
    const apiMethod = lowerFirst(parts.map(upperFirst).join(''));
    const apiKey = `${namespace}.${apiMethod}`;
    if (seenMethods.has(apiKey)) {
      throw new Error(`Target Wire methods produce a duplicate desktop API method: ${apiKey}`);
    }
    seenMethods.add(apiKey);
    return Object.freeze({ method, namespace, apiMethod });
  });
  return Object.freeze(prepared);
}

function groupMethods(
  methods: readonly PreparedMethod[],
): ReadonlyMap<string, readonly PreparedMethod[]> {
  const groups = new Map<string, PreparedMethod[]>();
  for (const method of methods) {
    const group = groups.get(method.namespace) ?? [];
    group.push(method);
    groups.set(method.namespace, group);
  }
  return groups;
}

function interfaceName(namespace: string): string {
  return `TargetDesktop${upperFirst(namespace)}ApiV1`;
}

async function renderPreload(
  source: TargetWireSource,
  methods: readonly PreparedMethod[],
): Promise<string> {
  const lines = [
    '// @generated by scripts/generate-target-preload.ts. Do not edit by hand.',
    "import { contextBridge, ipcRenderer, type IpcRendererEvent } from 'electron';",
    '',
    `const INVOKE_CHANNEL = ${JSON.stringify(source.TARGET_WIRE_INVOKE_CHANNEL_V1)};`,
    `const PUSH_CHANNEL = ${JSON.stringify(source.TARGET_WIRE_PUSH_CHANNEL_V1)};`,
    '',
    'interface TargetDesktopCall {',
    '  readonly requestId: string;',
    '  readonly input: unknown;',
    '}',
    '',
    'function invoke(method: string, request: TargetDesktopCall): Promise<unknown> {',
    '  return ipcRenderer.invoke(INVOKE_CHANNEL, {',
    '    wireVersion: 1,',
    "    kind: 'request',",
    '    requestId: request.requestId,',
    '    method,',
    '    input: request.input,',
    '  });',
    '}',
    '',
    'function onRunEventsAppended(callback: (push: unknown) => void): () => void {',
    '  const listener = (_event: IpcRendererEvent, push: unknown) => callback(push);',
    '  ipcRenderer.on(PUSH_CHANNEL, listener);',
    '  return () => ipcRenderer.removeListener(PUSH_CHANNEL, listener);',
    '}',
    '',
    `contextBridge.exposeInMainWorld(${JSON.stringify(source.TARGET_DESKTOP_API_GLOBAL_V1)}, {`,
  ];

  for (const [namespace, group] of groupMethods(methods)) {
    lines.push(`  ${namespace}: {`);
    for (const method of group) {
      lines.push(`    ${method.apiMethod}: (request: TargetDesktopCall) =>`);
      lines.push(`      invoke(${JSON.stringify(method.method)}, request),`);
    }
    if (namespace === 'run') lines.push('    onEventsAppended: onRunEventsAppended,');
    lines.push('  },');
  }
  lines.push('});', '');
  return format(lines.join('\n'), prettierOptions);
}

async function renderRendererTypes(methods: readonly PreparedMethod[]): Promise<string> {
  const lines = [
    '// @generated by scripts/generate-target-preload.ts. Do not edit by hand.',
    '',
    'import type {',
    '  PublicWireMethodV1,',
    '  WireFailureV1,',
    '  WirePushV1,',
    '  WireRequestV1,',
    '  WireSuccessV1,',
    "} from './wire.js';",
    '',
    'export type TargetDesktopCallV1<Method extends PublicWireMethodV1> = Pick<',
    '  Extract<WireRequestV1, { readonly method: Method }>,',
    "  'requestId' | 'input'",
    '>;',
    '',
    'export type TargetDesktopResponseV1<Method extends PublicWireMethodV1> =',
    '  | Extract<WireSuccessV1, { readonly method: Method }>',
    "  | (Omit<WireFailureV1, 'method'> & { readonly method: Method });",
    '',
  ];

  for (const [namespace, group] of groupMethods(methods)) {
    lines.push(`export interface ${interfaceName(namespace)} {`);
    for (const method of group) {
      lines.push(`  ${method.apiMethod}(`);
      lines.push(`    request: TargetDesktopCallV1<${JSON.stringify(method.method)}>,`);
      lines.push(`  ): Promise<TargetDesktopResponseV1<${JSON.stringify(method.method)}>>;`);
    }
    if (namespace === 'run') {
      lines.push('  onEventsAppended(callback: (push: WirePushV1) => void): () => void;');
    }
    lines.push('}', '');
  }

  lines.push('export interface TargetDesktopApiV1 {');
  for (const namespace of groupMethods(methods).keys()) {
    lines.push(`  readonly ${namespace}: ${interfaceName(namespace)};`);
  }
  lines.push('}', '');
  return format(lines.join('\n'), prettierOptions);
}

export async function buildTargetPreloadArtifacts(
  sourceValue?: TargetWireSource,
): Promise<TargetPreloadArtifacts> {
  const source = sourceValue ?? (await loadTargetWireSource());
  const methods = prepareMethods(Object.keys(source.PUBLIC_WIRE_METHODS_V1));
  if (methods.length === 0) throw new Error('Target Wire method registry is empty');
  return Object.freeze({
    preload: await renderPreload(source, methods),
    rendererTypes: await renderRendererTypes(methods),
  });
}

export async function generateTargetPreload(
  mode: GenerationMode,
  source?: TargetWireSource,
): Promise<void> {
  const artifacts = await buildTargetPreloadArtifacts(source);
  for (const [path, expected] of [
    [preloadPath, artifacts.preload],
    [rendererTypesPath, artifacts.rendererTypes],
  ] as const) {
    if (mode === 'write') {
      await writeFile(path, expected, 'utf8');
      continue;
    }
    const actual = await readFile(path, 'utf8').catch(() => undefined);
    if (actual !== expected) throw new Error(`Generated target preload drift: ${path}`);
  }
}

async function main(): Promise<void> {
  const flag = process.argv[2];
  if (flag !== '--write' && flag !== '--check') {
    throw new Error('Usage: tsx scripts/generate-target-preload.ts --write|--check');
  }
  await generateTargetPreload(flag === '--write' ? 'write' : 'check');
}

if (process.argv[1] !== undefined && resolve(process.argv[1]) === fileURLToPath(import.meta.url)) {
  await main();
}
